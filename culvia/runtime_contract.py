from __future__ import annotations


# Increment only when a Desktop Lite shell must reject an older Python service
# even when its importable dependencies still look complete.
DESKTOP_LITE_CONTRACT = 1
